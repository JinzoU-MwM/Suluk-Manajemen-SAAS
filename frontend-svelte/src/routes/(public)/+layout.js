// Public token deep-links (manifest, registration, package, contract) are
// client-rendered SPA pages — no SSR/prerender (dynamic, unauthenticated).
export const ssr = false;
export const prerender = false;
