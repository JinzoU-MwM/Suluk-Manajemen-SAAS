Latest session state before Git push:
- Removed Z.ai integration from backend (service + wiring + env example + OCR status test updates).
- Added OCR Review Queue + OCR Dashboard MVP with new models and endpoints.
- Added Duplicate Detection API endpoint for groups.
- Added Audit Log MVP model/service + integrations and /auth/audit endpoint.
- Added/updated tests and CI smoke gate entries for new features.
- Backend full suite currently passing (105 tests).
- Smoke gate command currently passing (13 tests).